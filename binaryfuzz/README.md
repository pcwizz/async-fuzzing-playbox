# Binary fuzzing approach

## Build notes

This has only been tested to build cleanly on x86_64 ubuntu. On aarch64 the build fails. It may be interesting to look into why but for the moment this distraction is not desired.
